import React from "react"

export default function({ id }) {
  //if( loading === false && resource === null ) <Button/>
  //else if( loading===true ) "?"
  //else - loading === false && resource - <Card />
  return <div>Card</div>
}
